# მკაცრი და არამკაცრი შედარების ოპერატორები Python-ში:

# **მკაცრი შედარების ოპერატორები:**
# - `==` (ტოლობა)
# - `!=` (არათოლობა)

# **არამკაცრი შედარების ოპერატორები:**
# - `<` (ნაკლები)
# - `>` (მეტი)
# - `<=` (ნაკლები ან ტოლი)
# - `>=` (მეტი ან ტოლი)